---
description: Staff a diff, PR, branch, file, or artifact with a LEAD and contrasting LENS.
argument-hint: "[diff, PR number, branch, file, artifact, or --with <slug>]"
---

Use the `temp-agency-review` skill on the provided review target.

Arguments:

`$ARGUMENTS`

