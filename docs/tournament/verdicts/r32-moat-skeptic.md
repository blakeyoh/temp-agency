## MATCHUP 9
WINNER: A
DECIDED BY: B's mechanism presupposes an artifact that does not exist — I checked `roster/franciscan-monk.md` and no "simplicity against obligation to the poor" fault line is named anywhere in it, so B's "named in its own profile" is a sourcing claim that fails on inspection, while A's transforms operate on files the repo actually has.

### Distance
VERDICT: A significantly better
REFERENCE: A sits at **High** ("forbid the answer from containing the problem's own vocabulary") and arguably past it on one dimension — it forbids the answer from containing the problem's own *evidence*, which is load-bearing where vocabulary is merely lexical; it does not reach Ceiling because the output remains recognizable as an answer without translation. B sits between **Low** and **Mid**: the output keeps every noun, every clause, every piece of support, and both halves of the split say the kinds of things the unsplit persona said. "Mildly unusual framing of the same answer" — twice.
PREDICATE: *Name a specific output this forbids.* A forbids: "This claim is well-supported by the three data points in paragraph four." The persona cannot write that sentence because the data points are not in its context — the clause-stripper removed them. A also forbids any output that quotes a superlative back at the monk, because the monk's artifact has had every superlative-bearing sentence deleted before it arrives. B forbids: "The monk recommends cutting the program." You get two voices instead of one verdict. Nameable, but a forbid on output *count*, not output *content*.

### Mechanism
VERDICT: A significantly better
REFERENCE: A sits at **High** ("a script that runs, whose output the model must consume") and leans toward Ceiling, because the model consumes the transform's output *instead of* the original rather than alongside it — there is no unconsented rejection, so it does not arrive. B sits at **Floor-to-Low**: "forcibly bisected" names no forcer. There is no fault-line registry, no splitter, no arbiter preventing re-convergence. It is a paragraph asking the model to try harder, wearing a structured-template costume.
PREDICATE: *Name the file that runs.* A: `transforms/investigative-journalist.py`, `transforms/franciscan-monk.py` — named in the packet's own text as `transforms/<slug>.py`. The journalist logs a discrepancy the packet does not disclose: `transforms/farmer.py` as specified ("re-sorts every line by how long it will still be true") **cannot be deterministic** — durability of a claim is a judgment, i.e. a model call. Two of three named exemplars are genuinely mechanical (regex superlative deletion, dependency-parse clause stripping); the packet calls all three "deterministic." B: **no file.** None named, none implied. Scores as exhortation.

### Irreducibility
VERDICT: A significantly better
REFERENCE: A sits at **High** — "withhold a fact from the context window; cannot be simulated by a model that knows it" — stated almost verbatim. The persona reads *only* the transformed artifact. B sits at **Low**: "hand the model a persona file — real, but promptable." B's description *is* a prompt; the packet's own paragraph, pasted into a chat window, produces the behavior.
PREDICATE: A base model given only the prompt `Read this document as an investigative journalist and list every claim that lacks support.` reproduces about **25%** of A — it will name some unsupported claims, but it can still see the support and will reflexively credit it; the *inventory* is ~40% reproducible, the *condition of reading with no access to support* is 0% reproducible. A base model given only the prompt `Split this persona into two halves that disagree along a fault line in its own profile, and do not resolve the argument.` reproduces about **80%** of B. This is my declared bias axis and it points at A, so I pressure-tested it: B's underlying observation (models resolve tension into a coherent register, and asking them not to yields mannered fake disagreement) is *true and hard to execute*. But hard-to-execute-by-prompt is a prompt-quality problem, not irreducibility, and B ships nothing to fix it. The bias holds after testing.

### Compounding
VERDICT: B slightly better
REFERENCE: A sits at the **Floor** — "identical on use #10 as on use #1." The transforms are fixed scripts; nothing in the packet describes rule growth. B sits at **Low** — "a novelty effect that fades as the pattern becomes familiar." A profile contains a finite number of fault lines; by use #10 you have seen them all and "the monk arguing with the monk" has become a register. This is a vote against my overall winner and I state it plainly: A is at the floor here, and the monk's objection lands — A's machinery is inert, it does exactly as much on its five-hundredth run as its first.
PREDICATE: *State what use #10 produces that use #1 does not.* A: **the same thing.** A stripped artifact, produced by the identical script, with the identical rules. Floor, and I say so. B: a fault line the reader has not yet seen split — until the profile's fault lines are exhausted, at which point B also produces the same thing. B buys perhaps six or eight uses of genuine variation, then joins A at the floor.

### Generative failure
VERDICT: A significantly better
REFERENCE: A sits between **High** and **Ceiling**. B sits at the **Floor** — "nothing; the pass silently produces the default answer" — and this is the worst position on the table because the failure is undetectable from the output.
PREDICATE: *Describe the exact failure output.* A, failure one: the clause-stripper mis-parses and item 7 of the numbered list reads `"which is why the Q3 number holds"` — a naked subordinate clause with its subject amputated. Unusable, and it names a hidden assumption: the document contained an argument whose conclusion was never stated as a claim, only implied by a connective. That is High. A, failure two: the monk transform deletes every superlative-bearing sentence and returns an **empty file**. That failure is itself a proposal worth acting on — "this document is entirely superlatives" — which is Ceiling. B: the bisection does not hold and the output reads `"Part of me values patience, but I also recognize the season imposes deadlines — the balanced approach is to phase the work."` A synthesized both-sides paragraph indistinguishable from the default answer. This is the journalist's named anti-pattern (false balance) generated automatically, and nothing in B detects it.

### ABSORPTION
LOSER'S STRONGEST MECHANISM: Bisect a persona along a fault line named in its own profile and ship the unresolved argument rather than the synthesis.
SAME-THESIS: FAIL — A's claim is that changing what the model can *see* changes the output; B's claim is that forbidding internal *resolution* changes the output. One is about input material, the other about output register. Two claims about why output is homogeneous, not one.
DELETION: FAIL — merely smaller. Delete the fracture six months from now and A still strips clauses, still hands each persona a mutilated artifact, still forbids "well-supported by paragraph four." The transforms have no dependency whatsoever on the reader being split. Nothing degrades; the repo just has fewer voices.
ONE-SENTENCE: FAIL — "Each persona reads a mechanically transformed artifact **and also** argues with itself along a fault line in its own profile." The conjunction is unavoidable because the two mechanisms act on different objects: A transforms the artifact, B fractures the reader.
DISPOSITION: ORTHOGONAL
NOTE: Two findings for the record. First, B's fault lines are not "named in its own profile" — the franciscan-monk profile names five Core Principles and four Anti-Patterns and states no tension between them; B requires a per-profile fault-line section that does not exist and that someone would have to author, which relocates B's novelty from the mechanism to the authoring. Second, A's own exemplar list smuggles a model-judgment transform (`farmer`: sort by how long a line stays true) in among two genuinely mechanical ones and calls all three "deterministic." The monk's live objection stands unresolved: 24 personas × one transform each is 24 files of machinery to accomplish what `sed` accomplishes twice — but the monk concedes the machinery *subtracts*, which is the monk's own Phase 1 executed in code, and that the deletion is irreversible within the pass, which is the one thing the monk cannot do by asking.

---

## MATCHUP 10
WINNER: A
DECIDED BY: B's failure mode — a procedurally clean deliberation whose minority report reads "None; the committee reached consensus" — launders the median answer as a rigorously deliberated result, which is worse than no mechanism at all, while A's failure mode tells you something true about your problem.

### Distance
VERDICT: A significantly better
REFERENCE: A sits at **High** and its intermediate artifact sits at the **Ceiling** verbatim — "the output is unrecognizable as an answer to the question until it is translated back." A is the only idea in this packet with a literal translation-back step. I am precise about which half hits ceiling: the intermediate output does, the delivered output does not, because the mapping is restored. B sits at **Mid** — "require the answer to come from a named non-obvious domain." The domain is parliamentary procedure and coalition theory, genuinely non-obvious and genuinely imported, but the answer keeps every noun and remains recognizable as a recommendation.
PREDICATE: *Name a specific output this forbids.* A forbids: "Leaderboards are inherently competitive, so lean into that — add tiers and badges." That solution exists only because the noun "leaderboard" carries gamification connotations; against MECHANISM_B it has nothing to attach to and dies. B forbids: "All specialists agree that the recommended path is X." A unanimous output cannot be produced, because a verbatim minority report is a required section. Real, and structurally enforced by the output schema — but a forbid on the output's *shape*, not on the space of answers it can occupy.

### Mechanism
VERDICT: A significantly better
REFERENCE: A sits at **High** — a script that runs whose output the model must consume, and "must" is literal here since the personas see only the abstracted text. The restore step implies a persisted mapping table, so there is state as well as a transform. B sits between **Low** and **Mid**: "a structured prompt template with required sections" (the minority report is a required section) shading into "a checklist the model self-verifies against" (does this proposal have a second? does this defection state a price?).
PREDICATE: *Name the file that runs.* A: the noun-substitution pre-pass and its mapping table — the packet describes both operations explicitly ("a transform strips its domain nouns," "only afterward is the mapping restored"), which requires a transformer and a persisted symbol table. Not given a path, but the two artifacts are entailed by the description rather than asserted. B: **no file.** Three protocol rules are named and none is given an enforcer. The journalist notes in B's favor that "a defection must state its price" is genuinely parseable — you could reject any dissent lacking a price clause — but the packet does not say anything does. Scores as a template, not a gate.

### Irreducibility
VERDICT: A slightly better
REFERENCE: A sits between **Low** and **Mid**. B sits at **Low**. Both are close to the promptable floor, and I say so directly against my own winner: abstraction-and-remap is a *named, documented technique*, one of the most promptable ideas in this entire packet. A's irreducible fraction is not the transformation — it is the enforcement that the reasoning pass never holds the original nouns, which is a context-window fact, not a prompt.
PREDICATE: A base model given only the prompt `Rewrite this problem statement replacing every domain-specific noun with a variable like ENTITY_A, solve the abstract version, then map the solution back.` reproduces about **55%** of A. The substitution and the abstract reasoning come out fine; what fails is that the same model still has the original nouns in context and leaks them back in ("MECHANISM_B — the leaderboard — should..."). A base model given only the prompt `Convene a six-expert panel on this; a proposal needs a second, defectors must state their price, and preserve a verbatim minority report.` reproduces about **70%** of B. Models perform panels readily and will happily act out coalition mechanics; what they produce is consensus theater, which is exactly B's failure mode, so the 70% reproduced includes the defect.

### Compounding
VERDICT: Tie
REFERENCE: Both sit at the **Floor** — "identical on use #10 as on use #1." Neither accumulates state. A applies the same substitution convention forever; B convenes a fresh committee with no memory of prior votes, alliances, or defections. B's minority report is "preserved verbatim **in the output**," not in the repo, so nothing carries forward. A arguably *decays* below floor: by use #10 the personas have learned the convention and begin back-translating ENTITY_A → users in their heads, restoring the priors the transform removed. B's committee varies across uses because the draw varies, but variety across uses is not the same as getting weirder with use, and the axis asks for the latter.
PREDICATE: *State what use #10 produces that use #1 does not.* A: **the same thing** — the same abstraction applied to a different problem, with the added liability that the reader has now learned the code. B: **the same thing** — a different committee on a different problem, with no faction history, no accumulated voting record, and no growing corpus of minority reports. Both at the floor, and I say so.

### Generative failure
VERDICT: A significantly better
REFERENCE: A sits between **High** and **Ceiling** on its primary failure and at the **Floor** on its secondary one; I name both rather than let the good example stand for the whole. B sits at the **Floor**, and below it in practical terms, because B's floor failure is *disguised as rigor*.
PREDICATE: *Describe the exact failure output.* A, primary failure (over-abstraction): the transform strips everything and the persona is handed `"How should ENTITY_A prevent BEHAVIOR_C on MECHANISM_B without reducing ENGAGEMENT_D?"`, and returns `"Introduce a cost on BEHAVIOR_C proportional to its detection probability."` Remapped, this is generic deterrence applicable to any problem — and that unusable output names a hidden assumption: the problem's apparent specificity was carried entirely by its nouns, and there was no structure underneath. That is a proposal worth acting on. A, secondary failure (under-abstraction): the transform leaves `"MECHANISM_B (the leaderboard)"` and the pass silently produces the default answer — Floor. B: six personas converge in one round — `"Second." "Second." "Agreed."` — and the required section reads `"Minority report: None — the committee reached consensus."` The pass produced the median answer and stamped it with procedural legitimacy. This is the single most dangerous output in the packet: it is the journalist's false-balance anti-pattern running in reverse, manufacturing the appearance of adversarial process around an unexamined default.

### ABSORPTION
LOSER'S STRONGEST MECHANISM: A verbatim minority report preserved as a required output section, which forbids shipping a consensus-only answer.
SAME-THESIS: FAIL — A's claim is that the model's domain priors fire on nouns before reasoning begins, so removing the nouns removes the priors. The minority report's claim is that multi-agent prompting converges to consensus, so disagreement must become the output format. One is about lexical priors in the input; the other is about convergence dynamics among agents. Second claim imported.
DELETION: FAIL — merely smaller. Delete the minority report six months from now and A still strips nouns, still forces reasoning against abstracted structure, still kills connotation-dependent solutions. A single persona reasoning about ENTITY_A has no need of a minority report to function; removing it removes a section, not a capability.
ONE-SENTENCE: FAIL — "Personas reason about a noun-stripped abstraction of the problem **and** preserve a verbatim minority report." The conjunction cannot be removed because the two mechanisms act at opposite ends of the pipeline — one on the input representation before reasoning, one on the output format after it.
DISPOSITION: ORTHOGONAL
NOTE: A's load-bearing claim is overstated and the packet does not disclose it: "removing the nouns removes the priors" is false as stated, because verbs and adjectives carry the priors too. "Users **game** the leaderboard to gain **unearned** status" becomes "ENTITY_A **games** MECHANISM_B to gain **unearned** status" — game, gain, and unearned survive intact and fire the same gamification prior the transform was built to suppress. The mechanism is real and the direction is right; the coverage claim is not sourced. The monk's objection is recorded and answered rather than smoothed: A's core move destroys haecceity — "users → ENTITY_A" erases the particular person, which is anti-Franciscan on its face — but because the mapping is *restored*, the monk classes it as a temporary fast from the particular rather than an erasure, which is a practice the tradition already contains.

---

## MATCHUP 11
WINNER: B
DECIDED BY: The governing frame — A is a filter on the existing generative process that reliably moves you from #12 to #13 and structurally never further, while B is a *different generative process*, which is what the frame says reaching #99 requires; A wins my own bias axis and I still vote against it on this ground.

### Distance
VERDICT: B slightly better
REFERENCE: A sits at **Mid** — "the answer may not be one we have already given" is a real structural constraint, stronger than "an unconventional angle," but it does not require the answer to come from anywhere in particular; it only fences off ground already walked. B sits between **Mid** and **High**, closer to High: "require the answer to come from a named non-obvious domain" understates it, because a farmer×monk hybrid with a polarity-reversed principle is a domain that *does not exist and has no priors at all*. "Reverse a principle's polarity" is the single most distance-generating operation in this packet — it manufactures positions no author would hold and no corpus contains.
PREDICATE: *Name a specific output this forbids.* A forbids: `"I suggest we add per-persona confidence scores."` — unless accompanied by an explicit resurrection statement naming what changed since `graveyard/confidence-scores.md`. Concrete and checkable, but note what it forbids: twelve things, in a repo that has tried twelve things. B forbids: `"Follow the incentives, therefore map the money first."` — the journalist's own signature move — because the child holds journalist Principles against monk Methodology, and subtraction-first does not produce follow-the-money. The child's output is not recognizable as any roster entry's voice. The monk objects that this is noise-distance rather than insight-distance, and that a polarity-reversed principle is a corrupted file rather than a perspective; the frame's explicit instruction ("efficiency is not the goal; exploration is") overrides the objection but does not refute it.

### Mechanism
VERDICT: B significantly better
REFERENCE: A sits at **Mid** at best — "a checklist the model is asked to self-verify against" ("does my proposal differ from the graveyard?"). Each tombstone is a structured template with a required cause-of-death field, which is Low; the differ-or-resurrect rule is a self-check, which is Mid. There is no diff, no gate, no rejection. B sits at **High** — "a script that runs, whose output the model must consume" — and beyond consume: the model must *become* the script's output. The scoring-and-promotion step adds a selection gate, though the packet does not say the gate is automatic.
PREDICATE: *Name the file that runs.* A: `graveyard/<slug>.md` — files, plainly, but no runner. "Mandatory reading" names no mandator; nothing rejects a proposal that ignores the graveyard. B: **`bin/breed <a> <b> --seed N`** — named as an executable, with arguments and a determinism seed, in the packet's own text, plus `roster/f1/` as its output directory. The journalist verified the splice is implementable today: `roster/investigative-journalist.md` has literally `## Core Principles`, `## Methodology`, `## Anti-Patterns`, and `## Voice & Tone` as headings, so section-crossover is markdown surgery. Two of three named mutations are pure text operations (delete a methodology phase; import a foreign Voice register); "reverse a principle's polarity" is a model call, not a script — one overclaim, disclosed. This axis's question is "does something execute," and one entry names a directory while the other names a binary.

### Irreducibility
VERDICT: A significantly better
REFERENCE: A sits at **High**. The ruler's High is "withhold a fact from the context window — cannot be simulated by a model that knows it"; A does the inverse operation on the same quantity, supplying facts that are nowhere in the weights, and the axis's actual question ("could the base model do this if simply asked nicely?") answers no for the same reason. You cannot prompt a model into knowing what this specific repo rejected in March and why. B sits between **Low** and **Mid**: "hand the model a persona file — real, but promptable," sharpened toward Mid because the mutated file *is* the persona, with no coherent original in context to regress toward.
PREDICATE: A base model given only the prompt `Propose a mechanism for this repo that we have not already tried.` reproduces about **20%** of A — it does not know what you tried, so it re-proposes dead ideas at roughly the base rate. A base model given only the prompt `Take the investigative-journalist and franciscan-monk profiles, use A's Principles with B's Methodology, interleave the Anti-Patterns, reverse one principle's polarity, and answer as the result.` reproduces about **45%** of B — the splice happens in-context fine; what fails is *commitment*, since a model asked to reverse "poverty clarifies" will soften it back toward sense ("that is, excessive austerity can create blind spots"), and only the on-disk file prevents the regression. **This is my declared bias axis, and it cuts against my overall winner. I am recording that plainly:** on irreducibility, the graveyard is the better idea in this matchup by two positions, and my vote for B is a vote against my own bias. The journalist's counter-pressure — that A's irreducibility is purchased with hand-authored content rather than produced by the mechanism — does not survive scrutiny, since deciding what to withhold is equally hand-authored at the High reference.

### Compounding
VERDICT: A slightly better
REFERENCE: A sits at **High** — "each use narrows the space of permitted answers" — stated almost verbatim, and it narrows *automatically*, since every rejection files a corpse. It does not reach Ceiling ("tightens automatically and **cannot be relaxed**") for one precise reason: the resurrection clause is an explicit, always-available escape hatch. That clause is the single thing keeping A off the ceiling of this axis. B sits between **Mid** and **High**: it accumulates a breeding pool that must be obeyed (promoted F1 personas get used), and generation 2 breeds from generation 1 survivors, so drift compounds — but the space *widens* rather than narrows, and the ruler's High is narrowing. I record the tension rather than resolve it: B better matches the axis's headline question ("does it get weirder with use") while A better matches the table the ruler declares authoritative, and the ruler governs.
PREDICATE: *State what use #10 produces that use #1 does not.* A: use #10 must dodge nine prior deaths that use #1 did not have to dodge — the tenth proposal is measurably more constrained than the first, and the constraint is cumulative and unavoidable without a written resurrection. B: use #10 draws from a pool containing F1 survivors of nine prior selections, so generation-2 children are two crossovers removed from any authored profile — output no author's hand is in. The journalist flags an unsourced load-bearing step: B's entire compounding claim rests on "scored," four words, with no scorer, no rubric, and no scale named.

### Generative failure
VERDICT: B slightly better
REFERENCE: Both reach **High**, and both touch **Ceiling**, but B reaches Ceiling by construction while A reaches it by interpretation. B's design *already treats* every failure as a proposal — score, then promote or kill — which is the Ceiling reference operationalized. A also has a genuine **Floor** failure that the packet does not acknowledge.
PREDICATE: *Describe the exact failure output.* A, high failure: at 60 tombstones the pass returns `"Proposal conflicts with graveyard/lens-transformers.md (cause of death: 'too much machinery'). Differentiate or resurrect."` against a proposal only superficially similar — a false-positive block that forces the author to articulate the actual distinction, which surfaces it. A, floor failure the journalist insists on recording: the pass runs *clean*, blocks exactly what it should by its own lights, and nobody notices the causes of death were wrong. B: `bin/breed farmer franciscan-monk --seed 7` deletes the methodology phase that held the profile together and reverses "poverty clarifies," and the child outputs `"The seasonal deadline is the only real constraint; accumulate what you need and do not stop to ask what is enough — the margin's view is a distraction from the harvest."` Unusable as advice, and it names a hidden assumption: the monk profile's subtractive stance was never argued for anywhere in the file, only asserted, and its reversal is internally coherent. It is also, straightforwardly, a proposal — an accumulationist agronomist is a real perspective this roster lacks, and B's pipeline is already built to promote it.

### ABSORPTION
LOSER'S STRONGEST MECHANISM: Every rejected idea is preserved with an authored cause of death, and future proposals must differ from the record or explicitly resurrect against it.
SAME-THESIS: FAIL — B's claim is generative ("asked to combine two personas, a model averages toward the generic; structural crossover produces combinations no author would choose"). The graveyard's claim is mnemonic ("models do not remember what did not work"). A record of dead children is *selection* pressure, not a generation mechanism; grafting it onto B imports a second claim about why output is homogeneous.
DELETION: FAIL — merely smaller. Delete the graveyard from B six months from now and `bin/breed --seed N` still produces mutants, scoring still promotes or kills, the roster still drifts. What B loses is that it re-breeds the same lethal combinations across generations — which is *waste*, and the governing frame states outright that efficiency is not the goal and exploration is. Under this frame, re-rolling a combination that died is not a defect. B gets slower, not worse. This is the test doing its job: the absorption is attractive precisely because it looks efficient, which is the thing the frame says not to buy.
ONE-SENTENCE: PASS — "Personas are bred by mutation under selection pressure from a record of prior deaths." One sentence, no conjunction. Recorded as a pass, and it does not rescue the absorption, since all three tests must pass.
DISPOSITION: ORTHOGONAL
NOTE: The finding worth keeping is about A and applies to any repo that builds a graveyard: **the cause of death is authored by whoever killed the idea.** Nobody files a tombstone reading "we killed this because the person proposing it was junior" or "because we were tired." The graveyard is institutional memory, institutional memory is written by whoever won the argument, and this repo's own journalist profile is trained to treat exactly that artifact as an official narrative requiring corroboration — yet A makes it *mandatory reading with binding force* and provides no mechanism for a dissenting death certificate. The monk's dissent from the verdict is also recorded: B is the accumulation instinct handed a breeding program, C(24,2) = 276 base pairs before seeds and mutations, and the monk's question "would less be enough?" has no answer inside B. The monk's grudging concession is that B has a kill step and A does not — A's founding premise is that nothing is ever discarded, which makes its mandatory reading a debt that grows without bound, while B at least contains the word "killed."

---

## MATCHUP 12
WINNER: B
DECIDED BY: A's differentiating claim — that a priced budget is "not a word-limit instruction" — is the entire basis of its novelty and is supported by nothing in the packet; strip the 1904 costume and A is `--max-words 40`, whereas B's gate works whether or not B's claim about self-visibility is true.

### Distance
VERDICT: B slightly better
REFERENCE: A sits at **Mid** — "require the answer to come from a named non-obvious domain," and 1904 telegraphy economics is exactly that; the historical framing carries real compression grammar (dropped articles, STOP, nominal style) beyond a bare count. A's ceiling is uncapped but unguaranteed. B sits at **High** and is structurally unlike the reference points: every entry on the Distance table is an *indirect proxy* for distance from the median, while B measures the quantity the axis is named after and enforces a floor on it. That is worth more than its position suggests. It does not reach Ceiling: nothing about B produces untranslatable output.
PREDICATE: *Name a specific output this forbids.* A forbids: `"There are three considerations here. First, ..."` — the tricolon preamble costs five dollars of a forty-dollar budget and buys nothing, so the economics kill it; more generally it forbids any restatement of the question. B forbids: `"Use a 2x2 impact/effort matrix to prioritize your backlog."` — if the auditor reconstructs that line as the no-skill answer and it appears in the real output, that section is rejected and regenerated. A direct, enforced forbid on the median itself. The countervailing fact, which I move to Compounding where it belongs: B guarantees you leave #1–#12 and lands you at #13, with no mechanism pushing further.

### Mechanism
VERDICT: B significantly better
REFERENCE: A sits between **Low** and **Mid** — a structured constraint the model self-verifies against by counting its own words. Nothing counts for it; nothing rejects it. B sits at the **Ceiling**, quoted almost verbatim by the packet: "a gate that rejects the output and forces regeneration without the model's consent" against "the overlapping sections are rejected and regenerated." B is the only entry in this entire four-matchup packet that reaches the Mechanism ceiling.
PREDICATE: *Name the file that runs.* A: **no file.** The price is stated in the prompt and enforced by the model's own restraint; there is no `wordcount` check, no budget rejector. Scores as exhortation with a period setting. B: the auditor subagent, its reconstruction artifact, and the overlap-threshold comparator. The journalist deducts honestly: **no path is named**, and the predicate asks for a file. But an overlap-past-a-threshold test is by definition an executable comparison with a numeric cutoff, so the component is entailed rather than merely asserted — which is more than A offers.

### Irreducibility
VERDICT: B slightly better
REFERENCE: Both sit low, and on my declared bias axis this is the weakest matchup in the packet. A sits at the **Floor-to-Low** boundary: it *is* a prompt, and a delightful one, which is a warning sign rather than a credential. B sits at **Mid** — "force output through a parser that rejects it; the model cannot opt out" — but only because of the gate; B's *reconstruction* step is highly promptable.
PREDICATE: A base model given only the prompt `You have forty dollars at one dollar per word. Send this analysis as a 1904 telegram.` reproduces about **75%** of A — base models do this well and enjoy doing it, and the period framing supplies telegraphic grammar for free. A base model given only the prompt `First write the answer a competent model with no skill installed would give, then rewrite your real answer to avoid overlapping it.` reproduces about **50%** of B. The journalist notes that this partly falsifies B's own load-bearing claim: B asserts "a model cannot see its own median from inside," yet a model asked to produce its own median produces a recognizable one. What is *not* reproducible is the threshold and the forced regeneration — a single model judging its own overlap grades leniently in its own favor every time. B's irreducible fraction is entirely in the gate, not in the insight. My bias's verdict on this matchup as a whole: A is closer to a costume than a claim, and B is a shaky claim with a real gate bolted to it.

### Compounding
VERDICT: A slightly better
REFERENCE: A sits at **Low** — "a novelty effect that fades as the pattern becomes familiar." Four media are named; by use #10 you have cycled all four twice and telegraphic compression has become a *register*, which is precisely the thing that wears off. B sits at the **Floor**: the auditor as described is stateless, receiving "the near-final output" fresh each time, storing no prior reconstructions, growing no forbidden zone. This is a vote against my overall winner and I mark it as such — B's stateless auditor is the more inert of two inert mechanisms.
PREDICATE: *State what use #10 produces that use #1 does not.* A: a slightly more practiced telegraphic register — which is a loss, not a gain, since practiced strangeness is style. Effectively the floor with one step of decay above it. B: **the same thing** — the same threshold check against a freshly generated median. And here the structural cap belongs: even if the auditor *did* accumulate, B pushes only just past its threshold each time, so use #10 sits exactly threshold-far from whatever the median then is. That is a treadmill, not a ratchet — B is a machine for reliably reaching #13 forever, and under the governing frame that is its most serious defect.

### Generative failure
VERDICT: A significantly better
REFERENCE: A's primary failure sits at the **Ceiling** — "the failure is itself a proposal worth acting on" — and is the best failure mode in this packet; A also carries a **Floor** failure I name rather than hide. B sits at **Mid** and below it in practice, because its noise is expensive (each round is a full regeneration) and, worse, *invisible in the artifact*.
PREDICATE: *Describe the exact failure output.* A, primary failure: the budget is spent and the deliverable reads `"ROADMAP UNSOUND STOP PHASE TWO UNSCOPED STOP COMPETITOR RISK BURIED STOP RECOMMEND DELAY STOP"` — thirteen words, thirteen dollars, under budget, every nuance gone. It is a failure *as a deliverable* and a proposal *as a finding*: it says the persona's full-length analysis contained exactly four claims and thirty sentences of padding. A, secondary failure: the model overruns the budget, silently self-corrects, and ships normal-length prose with telegraphic flavor — Floor. B: reconstruction and output overlap at 80%, sections are rejected, regenerated, overlap at 78%, regenerated again, and the loop terminates either on a retry cap (shipping the original with a warning) or by producing text beaten into non-median strangeness — `"sort the backlog by a bespoke composite index of urgency-weighted stakeholder salience"` — which is novel, worse, and unfalsifiably off-median. Nothing in the output tells a reader which of those two things happened.

### ABSORPTION
LOSER'S STRONGEST MECHANISM: A priced delivery budget where what the persona sacrifices under real cost reveals what it actually believes.
SAME-THESIS: FAIL — B's claim is that a model cannot see its own median from inside, so an external agent must generate it. A's claim is that models treat "be concise" as a style note, so a budget must be posed as an optimization problem. One is about self-visibility of the median; the other is about the difference between a style instruction and a constraint with a cost function. Two claims.
DELETION: FAIL — and in the sharper direction. Delete the price from B six months from now and the auditor still reconstructs the median, still measures overlap, still gates. B gets longer, not worse — and a longer output gives the auditor *more* surface to catch. Running the tests properly reveals the merge is actively harmful: short outputs overlap the median **more**, because there are fewer ways to say a thing in thirteen words, so the price would make B's gate fire constantly on correct-and-unavoidable phrasings. B is worse *with* the absorbed part, which is the strongest possible refusal this test can return.
ONE-SENTENCE: FAIL — "An adversarial auditor rejects any section overlapping the median it reconstructs, **and** the persona delivers what survives under a priced word budget." The conjunction cannot be removed because the two mechanisms act on different quantities: one on length, one on overlap. No single predicate covers both.
DISPOSITION: ORTHOGONAL
NOTE: The finding worth keeping is a defect in the winner. **B's gate cannot distinguish "median because unimaginative" from "median because correct."** It rejects overlap, and correct-and-obvious answers overlap by definition, so B systematically punishes right answers for being reachable — and because the auditor is itself a model with the same priors, the "median" it generates is its own default, meaning the threshold measures agreement between two samples from one distribution and calls it homogeneity. Second, for the record: A's central differentiator is unsupported. The packet asserts "the economics of the medium, not a word-limit instruction" and supplies no mechanism for the difference; the strongest exemplar is the woodblock, because a fixed glyph count is not a word count and carving is irreversible, and the weakest is the telegram, which is forty words with a hat on. The monk's judgment is the one this panel most wants preserved: A is **romantic poverty** — the anti-pattern named explicitly in the monk's own profile, "conflating voluntary simplicity with imposed deprivation" — and telegrams, index cards, and woodblocks are its costume. The monk concedes in the same breath that A's underlying epistemology is the monk's own ("what a persona sacrifices under real cost reveals what it actually believes" is "poverty clarifies" restated), and refuses to resolve the split: A gets the epistemology right and the apparatus wrong, and both halves are in the entry. Between a costume and a machine, the monk takes the machine only because the machine removes something.

## PANEL NOTE

> Returned by the panel after matchup 12, outside the per-matchup format. Preserved
> verbatim under its own heading so the parser reads it as commentary, not as part of
> matchup 12's absorption record.

Panel note across all four matchups, since it bears on the record: every winner names an executable and every loser is a prompting maneuver. The panel checked whether it was simply rewarding files, and found the correlation is in the packet rather than in the judging — the four losers also lost on Distance, Irreducibility, and Generative failure, and three of the four fail silently to the default answer, which is the Floor of the axis that matters most for a repo trying to get past #12.
