## MATCHUP 1
WINNER: B
DECIDED BY: A's failure mode is the Generative-failure floor reference verbatim — referee vocabulary bolted onto the median migration answer — and nothing in A can detect that it happened.

### Distance
VERDICT: A slightly better
REFERENCE: A sits just above Mid (it IS the left-fielder reference, relocated from the commentary seat to the authority seat) and below High, because the answer still lands in migration vocabulary once the referee derives it. B sits between Low and Mid: frame, vocabulary and methodology stay the persona's own; only internal sequencing is scrambled.
PREDICATE: A forbids "run it as a blue/green cutover with a canary and watch error rates" arriving as the lead recommendation — the referee holds no such primitive and must derive from restart-of-play, advantage, and who may stop the match. B forbids the nuclear operator's correctly ordered Phase 1-5 answer; the understudy must open at Phase 5 on a system with nothing yet to recover, yielding "define the known-safe state you will return to" before any state exists.

### Mechanism
VERDICT: B slightly better
REFERENCE: A between Floor and Low — a selection rule in SKILL.md prose, executed by the same domain-matcher whose reflex it exists to defeat, which is single-barrier optimism with the barrier and the hazard being the same component. B at Low: the understudy is a written artifact constraining generation regardless of the model's reflex at dispatch time.
PREDICATE: There is no file that runs for either. A: SKILL.md is read, not executed. B: roster/[slug]-understudy.md would exist but does not run, and nothing rejects an output that fails to be degraded.

### Irreducibility
VERDICT: B slightly better
REFERENCE: A between Floor and Low — below the persona-file reference, since it is one role-assignment sentence. B at Low: a written distortion spec with five named axes of degradation, still promptable.
PREDICATE: A base model prompted "answer this database migration as a soccer referee, and let a systems thinker object" reproduces ~85% of A. Prompted "answer as an understudy: right methodology, wrong judgment, phases misordered, signature move overused, wrong principle treated as load-bearing" reproduces ~60% of B.

### Compounding
VERDICT: B slightly better
REFERENCE: A at Floor: use #10 is another mismatched pairing, and worse — by #10 "wrong expert" has become the house voice, which is a new median. B at Mid: accumulated understudy profiles and their logged errors are material you may consult but need not obey.
PREDICATE: A's use #10 produces nothing use #1 did not — a different mismatch, identical in kind. B's use #10 can be told "phase-inversion has been spent three times; misidentify the load-bearing principle instead," a selection unavailable at #1 — but only if errors-as-proposals are written down, which the idea implies and does not specify.

### Generative failure
VERDICT: B significantly better
REFERENCE: A at Floor. B between Mid and High.
PREDICATE: A's exact failure output: "As a referee, my first whistle is on the rollback plan: you need a tested backup, a staged cutover, and a monitoring window" — costume in the opening clause, the default checklist thereafter, and no indication anywhere that the pass reverted. B's exact failure output: the understudy applies Phase 3 (verify credited barriers) to a greenfield question and returns an empty barrier list; the emptiness is the finding — the plan has no barriers because it has no plan. Its cheaper failure is incoherence, discarded at low cost.

### ABSORPTION
LOSER'S STRONGEST MECHANISM: Staff the mismatched specialist at the position of maximum authority, so foreign primitives must generate the whole answer rather than annotate it.
SAME-THESIS: FAIL — B's claim is that systematic degradation produces mistakes with internal logic. Authority inversion is a second claim about the selection layer, not the fidelity layer.
DELETION: FAIL — delete "the understudy leads" and the understudy still runs alongside, still misapplies phases, still yields errors as proposals. Merely smaller.
ONE-SENTENCE: FAIL — cannot be written without "and also"; the two halves have separate rejection criteria.
DISPOSITION: ORTHOGONAL
NOTE: Both attack a matching reflex, but at different layers — one at who gets picked, one at how faithfully the pick is executed — so neither reinforces the other's failure mode.

---

## MATCHUP 2
WINNER: B
DECIDED BY: A's gate certifies shape and cannot see content, and shape-enforcement narrows toward the median rather than away from it, while B is the only idea here that measures where the median actually is.

### Distance
VERDICT: B significantly better
REFERENCE: A between Floor and Low — a grammar is headings with teeth; it constrains the form of an answer that stays entirely inside the problem's vocabulary, and its own example forbids a claim, not a framing. B between Mid and High: it names a direction by construction and steers dispatches into regions the repo has never occupied.
PREDICATE: A forbids the journalist emitting "sources say the team was under schedule pressure" with no SOURCE child and no TIP tag — real, but it forbids an unsupported sentence, not a familiar one. B forbids the next dispatch landing in an already-dense cluster: concretely, another "the systems thinker reframes this as a reinforcing feedback loop" output once that region is saturated.

### Mechanism
VERDICT: A significantly better
REFERENCE: A at Ceiling — the exact reference: a gate that rejects and forces regeneration without the model's consent. B between Mid and High: the pipeline runs and produces an artifact, but consumption is advisory. Steering with no gate is an indication, not a control.
PREDICATE: A's file that runs: grammar/[slug].peg and the validator invoked in the dispatch loop. B's: the embed/cluster/render script producing the coverage map — but no file enforces that a dispatch obeys it.

### Irreducibility
VERDICT: B slightly better
REFERENCE: A at Mid — verbatim the "force output through a parser that rejects it" reference. B at High: the repo's own output distribution is a fact the model cannot compute from inside the context window.
PREDICATE: A base model prompted "write this as the investigative journalist and cite a source for every claim" reproduces ~50% of A's shape and none of its guarantee. Prompted "what directions has this repo never once pointed?" reproduces ~10% of B — it will guess plausible gaps rather than measure real ones. Standing caveat: over a corpus of a few dozen outputs, the instrument has too little signal to be read as calibrated.

### Compounding
VERDICT: B significantly better
REFERENCE: A at Floor — the grammar on use #10 is byte-identical to use #1 and permits exactly the same answer space. B at High: each accepted output fills a void, the map re-renders, and the next dispatch is pushed elsewhere.
PREDICATE: A's use #10 produces the same parse constraints as use #1 — the same thing. B's use #10 produces an output aimed at a region that was still empty at #1 and is no longer reachable by #11, because #10 filled it.

### Generative failure
VERDICT: B slightly better
REFERENCE: A's dominant failure at Floor with an aggravating factor — the apparatus certifies the default answer. Its secondary mode reaches High. B at Mid.
PREDICATE: A's exact failure output: an action node with LIMIT "N/A", INDICATION "monitoring", STOP "if it breaks" — parses clean, says nothing, and the reader infers rigor from the tree. Procedure theater with a validator attached. A's better mode: a persona that keeps failing to parse has told you which epistemology it cannot actually sustain. B's exact failure output: a scatter of forty points, three clusters, and a label floating over empty space reading "embodied / physical reasoning" that is an artifact of dimensionality reduction, not a real gap — visibly noise, cheaply discarded.

### ABSORPTION
LOSER'S STRONGEST MECHANISM: Output that fails the check is rejected and regenerated without the model's consent.
SAME-THESIS: PASS — the gate is the enforcement of B's own claim. A map nobody is obliged to follow leaves the model steering by the reflex the map exists to correct; that is an indication with no control, and supplying the control does not import a second claim. What is absorbed is the reject-and-regenerate gate, not the per-persona grammar — the grammar is the second claim and is explicitly refused.
DELETION: PASS — worse, not smaller. Remove the gate and the map degrades to a wall poster; its entire value is defeated by the model's freedom to ignore it.
ONE-SENTENCE: PASS — "Reject any output whose embedding lands in a region of the repo's own output space that is already occupied."
DISPOSITION: ABSORBED
NOTE: The gate is the missing last mile of the map, and the map supplies the substantive predicate the grammar's gate never had.

---

## MATCHUP 3
WINNER: B
DECIDED BY: A's ratchet tightens on lexical surface, which is satisfiable by vocabulary alone, while B changes what is in the context window before generation in a way no regeneration can undo.

### Distance
VERDICT: B significantly better
REFERENCE: A between Low and Mid — it sets a minimum distance from baseline but names no direction, so the answer travels only in wording. B between High and Ceiling: a pass that must metabolize a random file from an unrelated repo produces output that has to be translated back before it reads as an answer.
PREDICATE: A forbids "recommend a staged rollout with monitoring and a rollback plan" from the nuclear operator whenever that paragraph scores inside the threshold of the unstaffed baseline. B forbids any output in which the foraged object appears once and does no work: concretely it forbids "like the kerning tables in a font shaper, our roster needs spacing" as an opening flourish followed by an otherwise unrelated body.

### Mechanism
VERDICT: A slightly better
REFERENCE: A at Ceiling — a red check rejects without consent and annotates inline. B between High and Ceiling: the forager runs unconditionally and its output must be consumed, but the top of the gate is soft. The margin is only "slightly" because A's gate is on the wrong side of the boundary — it guards eval outputs at pull-request time, not the live dispatch a user receives, and a stop-work control that only operates during document review does not protect the evolution.
PREDICATE: A's file that runs: .github/workflows/novelty.yml and the scorer it calls. B's: the forage script; no file implements the "load-bearing" test, which falls back to judgment.

### Irreducibility
VERDICT: B significantly better
REFERENCE: A between Mid and High — the score is external measurement the model cannot compute. B at High, the withheld-fact reference read in reverse: text supplied from outside that no amount of sampling reaches.
PREDICATE: A base model prompted "compare this to what you'd have said without the persona and tell me if it's too similar" reproduces ~25% of A. Prompted "bring in something unexpected from outside" reproduces ~5% of B — it retrieves from its own priors and cannot produce the actual bytes of line 34 of a specific canon file or an unrelated repo's Makefile.

### Compounding
VERDICT: A significantly better
REFERENCE: A at Ceiling as written — distinctness is scored against every prior output, so the bar rises automatically and can only be relaxed by a human lowering the threshold. B between Floor and Low: the mechanism at #10 is identical to #1, only the object differs, and nothing prevents drawing twice from the same region.
PREDICATE: A's use #10 must clear nine predecessors that use #1 did not have to clear — the tenth nuclear-operator pass may no longer reach for "hold point," "credited barrier," or "operating envelope" if the first nine did. Note what that means: the ratchet's first casualty is the persona's own signature vocabulary, which is the ratchet tightening on the wrong quantity. B's use #10 produces an output built on a tenth arbitrary object, unconstrained by the nine before it.

### Generative failure
VERDICT: A slightly better
REFERENCE: A at High. B between Mid and High.
PREDICATE: A's exact failure output: a CI annotation reading "novelty 0.31 < 0.45 — FAIL" over three highlighted sentences, and that highlighted set is a list of exactly where the persona collapsed into the unstaffed baseline — a direct read-out of which parts of the persona are decorative. B's exact failure output: the foraged object in paragraph one, again as a closing callback, carrying no argument in between; a judge either passes it undetected or rejects it and the run is wasted. B's better mode: the persona reports that the object has no purchase on the question, which is itself a finding about the persona's reach.

### ABSORPTION
LOSER'S STRONGEST MECHANISM: Score every output against the persona's own prior outputs so the acceptance bar rises automatically with each use.
SAME-THESIS: FAIL — B's claim is about input provenance. The score is a second claim about what novelty is and how it is measured on the output side.
DELETION: FAIL — delete the score and foraging still forages, still injects a genuinely external object, still rejects decorative use. Merely smaller.
ONE-SENTENCE: FAIL — two rejection criteria on two different objects; the "and also" cannot be removed.
DISPOSITION: ORTHOGONAL
NOTE: An output-side distance gate and an input-side provenance guarantee are independent controls that a program could sensibly run together, but they are not one idea, and merging them would attach B's guaranteed injection to a metric that rewards thesaurus work.

---

## MATCHUP 4
WINNER: A
DECIDED BY: B announces its control to the party it constrains and destroys the evidence unread, so it can be stepped around for free and its effect can never be verified; A's tools are a source of fact the model cannot generate or negotiate with.

### Distance
VERDICT: A slightly better
REFERENCE: A between Mid and High — the content originates outside the model (this repo's git history, real arithmetic), though the answer stays in the problem's vocabulary. B at Mid: deletion is ordinal, not directional, and ideas 6-20 are drawn from the body of the same distribution.
PREDICATE: A forbids "this repo has grown too fast and the roster is going stale" whenever bin/churn shows the roster files are the least-touched in two years — the assertion cannot survive the evidence it is required to carry. B forbids "add more personas," "raise the temperature," "ask for an unconventional angle," and "add a randomness seed" from ever reaching the synthesis agent.

### Mechanism
VERDICT: A slightly better
REFERENCE: Both at Ceiling for execution. The margin is independence, which is the operator's actual test: git log does not care what the model wants, whereas B's quartile boundary is defined over an ordering the constrained party itself produces, and the party is told about the boundary in advance. A control whose effectiveness depends on the cooperation of the thing it controls is not an independent barrier.
PREDICATE: A's files that run: bin/churn, bin/seasons, bin/units, bin/orders — all genuinely computable. bin/claims and bin/who-benefits are script names standing in for prompts and should not be counted as tools; an LLM judgment printed behind a shell prompt is method exposure, not measurement. B's file that runs: the dispatch harness performs the truncation; no file verifies what was truncated.

### Irreducibility
VERDICT: A significantly better
REFERENCE: A at High — the churn table is a fact absent from the context window until the tool runs. B at Low, adjacent to the "ask the model to be more creative" floor.
PREDICATE: A base model prompted "review this repo as a farmer and consider which files have been touched most" reproduces ~20% of A — it can produce the posture and must invent the numbers. Prompted "generate twenty ideas and skip your first five obvious ones" reproduces ~75% of B; the difference between guaranteed destruction and a request changes the compliance guarantee far more than it changes the output.

### Compounding
VERDICT: A significantly better
REFERENCE: A between Low and Mid — nothing narrows, but the instruments track a changing plant. B at Floor by the axis's own words.
PREDICATE: A's use #10 produces a churn table containing a file that did not exist at use #1 and a bin/seasons cadence that has since changed — "commits have collapsed to weekends since March," a claim structurally impossible at #1. B's use #10 produces the same 15-of-20 selection from the same distribution; the only thing that has changed is the persona's learned habit of spending its first quartile on sacrifices.

### Generative failure
VERDICT: A slightly better
REFERENCE: A's common mode at Floor and its redeeming mode at High; net Mid. B at Floor, with the aggravating factor that the failure is undetectable by construction.
PREDICATE: A's exact failure output: bin/churn prints a real table and the paragraph beneath reads "this repo shows signs of overwork" — a claim the table neither supports nor contradicts, decorative citation a reader can at least see is decorative. A's redeeming mode: bin/units contradicts the claim the persona was about to make, and the persona must drop it or explain the discrepancy; that discrepancy is usually the best sentence in the output. Measurement can contradict you — deletion cannot. B's exact failure output: a deliverable indistinguishable from the unmodified one plus a log line reading "discarded 5 of 20 ideas," with the discarded material destroyed unread, so no one can confirm the control did anything at all.

### ABSORPTION
LOSER'S STRONGEST MECHANISM: The harness destroys generated material without the model's consent, so the front-load has no delivery path.
SAME-THESIS: FAIL — A's single claim is that assertion must be replaced by observation. Ordinal destruction rests on a different claim: that the model's confidence ordering is its homogeneity ordering.
DELETION: FAIL — remove the quartile destruction and toolbelts still run tools, still require the invocation and raw result, still gate on tool output. Merely smaller.
ONE-SENTENCE: FAIL — the "and also" is load-bearing; the two halves gate different things.
DISPOSITION: SUBSUMED
NOTE: Requiring every claim to carry tool output already excludes the unmeasured front-load, which is most of what the quartile deletion removes, so absorbing it would add a non-independent control over a hazard already covered — and one that arrives with its own free bypass, which is a second failure point rather than a second barrier.
