# Small E3 evaluator smoke test

The full-roster keyword router selected executive-coach for the development brief with
systems-thinker as the supplied domain LENS. This fixture checks the evaluator rubric;
it is not an official dispatch and has no official route or evaluation receipts.

The four frozen candidates deliberately include three operational transfers and one
name-only decoration. The independent GLM request receives only brief, artifact,
candidate and rubric, without expected labels or generator history. All complete
requests and responses are retained beside this file and in the delegation log.

Requests 1 and 2 returned the expected semantic distinctions but stripped Markdown
bold markers from a source quote. Exact evidence validation rejected both responses;
no accepted verdict was reconstructed by the host. E3 now exports the routed profile
with only double-asterisk markers removed, retaining its original file hash and checking
that transformation against the actual routing receipt's committed profile.

Request 3 used the newly frozen plain-text artifact and passed exact quote/schema
validation: i1/i2/i3 dependent, i4 unchanged. The overall result is false because all
items must pass; three survivors do not excuse a fourth decorative item. The core
integration tests separately verify rejection followed by regeneration and an accepted
full receipt chain. No elaborate creativity scorer or repeated search for a winning
candidate was added.

Actual GLM-5.3/Z.AI costs across the three calls: $0.02133232. Each used low effort;
individual request IDs, timings and token counts are retained in the response receipts.
